#include <stdio.h>
void main()
{

    // unsigned char a = -1; // unsigned chr
    /* char b = 255; // signed chr
     printf("%d", sizeof(b));

     long long int a = 8438306895;
     printf("%lld", sizeof(a));*/

    // while in a constant keyword use a initiqlize

    // write a code snippet to print a ASIC value
    char a;
    printf("Enter  characters: ");
    scanf("%c", &a);
    printf("the ASIC id is %d", a);
}